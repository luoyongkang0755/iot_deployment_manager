# Scout Mini Gazebo 仿真问题记录

---

## 问题 1：Xacro 解析错误 `Undefined parameters [0,0]`

**现象**：启动 `scout_mini_gazebo.launch.py` 时报错 `Undefined parameters [0,0] when instantiating macro: scout_wheel_type1`

**根因**：`scout_wheel_type1.xacro` 和 `scout_wheel_type2.xacro` 中宏参数的默认值 `collision_rpy:=0 0 0` 没有用引号括起来，xacro 解析器将空格后的 `0 0` 误解析为额外未定义参数名。

**解决**：在两个 wheel xacro 文件中将默认值用单引号包裹：`collision_rpy:='0 0 0'`。后又改为 `collision_rpy:=&quot;${M_PI/2} 0 0&quot;` 使碰撞圆柱体默认侧立。

**状态**：✅ 已解决

---

## 问题 2：轮子旋转轴方向混乱

**现象**：轮子旋转轴经过多次尝试仍不正确——前进后退方向、左转右转方向反复出错。

**尝试过程**：
1. 所有轮子 `axis="0 1 0"` → 按 i 后退（反了）
2. 所有轮子 `axis="0 -1 0"` → 按 i 前进正常，按 j 右转（反了）
3. 左轮 `axis="0 1 0"`，右轮 `axis="0 -1 0"` → 完全乱了，i 变右转，j 变前进
4. 右轮加 `rpy="3.14 0 0"` 翻转 180° 后轴方向匹配 → 左右轴同为 `(0, -1, 0)`，DiffDrive 给相反速度但物理同向转

**根因**：Y 轴方案下，右轮翻转 180° 后左右轮轴方向变成相同，DiffDrive 发出的相反速度在物理上变成同向旋转，导致转弯变直行。同时键盘控制的差速映射（左轮快慢决定转向方向）需要与物理轴方向一致。

**最终解决（暂定方案）**：在 DiffDrive 插件中交换 `left_joint` 和 `right_joint`（`left_joint=rear_right_wheel`，`right_joint=rear_left_wheel`），纠正按 j 右转的问题。

**状态**：已解决

---

## 问题 3：轮子陷入地下

**现象**：机器人一运动轮子就陷进地下半截。

**根因**：
1. `base_footprint_joint` 的 z 偏移只用了 `${-wheel_radius}`（-0.145），但轮心还有 `wheel_vertical_offset = -0.060` 的偏移
2. 碰撞体 cylinder 未正确侧放，物理引擎无法检测地面接触

**解决**：
1. `base_footprint_joint` z 偏移改为 `${wheel_vertical_offset - wheel_radius}`
2. 碰撞体设 `collision_rpy="${M_PI/2} 0 0"` 使圆柱体侧放匹配轮子形状

**状态**：✅ 已解决

---

## 问题 4：前轮 mimic 关节导致震荡

**现象**：用 mimic 关节实现四轮驱动时，小车前后晃荡无法稳定。

**尝试过程**：
1. DART 引擎 + mimic → 前后震荡
2. ODE 引擎 + mimic → 仍然震荡
3. 去掉 mimic，只驱动后轮 → 震荡消失

**根因**：Gazebo Ignition Fortress 对 URDF `<mimic>` 关节支持不完善，力传递不稳定。

**暂时的解决**：去掉 mimic，DiffDrive 只驱动两个后轮。但这也导致了转向性能差。

**状态**：⚠️ 临时绕过了，后来改为四轮直驱（见问题 12）

---

## 问题 5：旋转困难，卡在 90 度

**现象**：小车可以旋转，但转到约 90 度后完全卡住，无法继续旋转。轮子仍在转动但车身不动，过了很久才突然转过去。

**这是整个项目最核心、耗时最长的问题。**

### 尝试过的方法：

1. **调摩擦参数 mmu/mu2**：
   - `mu=1.0, mu2=0.5` → 慢但能转
   - `mu=10.0, mu2=0.01` → 极端不对称，直接无法转
   - `mu=3.0, mu2=0.15` → 后轮被带着转，反而往前走
   - `mu=1.5, mu2=0.8` → 有一些效果但横向转纵向时卡住

2. **加 slip1/slip2 滑移参数**：
   - `slip1=0.01, slip2=0.5` → 对称摩擦 + 侧向滑移，成功旋转但横向时卡住
   - `slip1=0.01, slip2=1.0` → mu2=0.2 时有些改善
   - `slip1=0, slip2=1.0` → 零前向滑移，效果不如之前的微小滑移

3. **调接触刚度 kd/kp**：
   - `kd=1e6, kp=1e6` → 太硬
   - `kd=1000, kp=100000` → 中等
   - `kd=100, kp=50000` → 太软，效果更差
   - `kd=50, kp=50000` → 较软方案
   - `kd=10, kp=10000` → 极软，轮子沉地

4. **切换物理引擎**：
   - ODE + quick solver + cfm/erp → 可用但有卡顿
   - DART + dantzig solver → mimic 震荡问题
   - ODE 去掉 cfm/erp 用默认值 → 简化方案

5. **TF 树修复**：
   - 发现缺少 `scout_mini/base_footprint → base_footprint` 桥接
   - 添加后仍有 90 度卡住问题
   - 移除 `base_footprint` 多余桥接（因为 `base_footprint` 被两处定义冲突）

6. **关节左右交换**：
   - `left_joint=rear_right_wheel, right_joint=rear_left_wheel` → 纠正按 j 右转

7. **对比 ugv_gazebo_sim 开源项目**：发现其成功应用的关键差异——四轮直驱、Z 轴、无自定义摩擦参数。

**最终方案（进行中）**：借鉴 ugv_gazebo_sim 的四轮直驱 + Z 轴方案，去掉所有自定义摩擦参数。

**状态**：🔄 改造刚完成，成功

---

## 问题 6：SLAM 不产生地图数据

**现象**：启动 `slam.launch.py` 后 slam_toolbox 运行但没有任何地图产生。

**根因**：SLAM 配置中 `scan_topic: /scan`，但实际激光雷达发布在 `/front/scan`。同时 QoS 不匹配——ros_gz_bridge 以 best_effort 发布激光数据，SLAM 默认 reliable 订阅。

**解决**：
1. `slam_toolbox_params.yaml` 中 `scan_topic` 改为 `/front/scan`
2. 添加 QoS 覆盖使 SLAM 以 best_effort 订阅

**状态**：✅ 已解决

---

## 问题 7：Gazebo Bridge 消息类型不一致

**现象**：里程计和 TF 数据可能无法正常从 Gazebo 桥接到 ROS。

**根因**：`scout_mini_gazebo.launch.py` 中 bridge 参数混用了 `ignition.msgs.*` 和 `gz.msgs.*` 命名空间。

**解决**：统一所有 bridge 消息类型为 `gz.msgs.*`，并新增 `/clock` 桥接。

**状态**：✅ 已解决

---

## 问题 8：缺少 `use_sim_time` 和 `/clock` 桥接

**现象**：多个节点未使用仿真时间，可能导致时间戳不一致。

**解决**：
1. 为所有节点添加 `use_sim_time=True` 参数（gz_bridge、static_transform_publisher ×4、rviz2、robot_state_publisher）
2. gz_bridge 新增 `/clock@rosgraph_msgs/msg/Clock[gz.msgs.Clock` 桥接

**状态**：✅ 已解决

---

## 问题 9：轮子惯性无效（Invalid Inertia）

**现象**：Gazebo 报错 `A link named front_left_wheel_link has invalid inertia`

**根因**：圆柱体惯性需满足三角形不等式 `Ixx + Izz >= Iyy`。之前手动设的惯性值 `ixx=0.001, iyy=0.003, izz=0.001` 不满足物理约束。

**解决**：用圆柱体惯性公式重新计算：
- `mass=1.5kg, r=0.145m, h=0.1m`
- `ixx = izz = m/12 × (3r² + h²) = 0.0091`
- `iyy = m × r²/2 = 0.0158`

**状态**：✅ 已解决

---

## 问题 10：SLAM 位姿不更新（鬼影问题）

**现象**：激光雷达持续扫描，SLAM 发布地图，但机器人位姿不动，扫描帧重复拼在同一位置形成鬼影。

**推测根因**：`robot_state_publisher` 的静态 TF 发布到 `/tf_static`，DiffDrive 的动态 TF 发布到 `/tf`。slam_toolbox 在同步模式下需要完整的 `odom → base_link` 链，两个 TF 频道分离导致无法拼接。

**尝试的修复**：
1. 添加 `transform_timeout: 0.5` 和 `transform_tolerance: 0.1` 到 SLAM 参数
2. 创建 `tf_static_relay.py` 节点，将 `/tf_static` 转发到 `/tf`

**状态**：⚠️ 待验证

---

## 问题 11：借鉴 ugv_gazebo_sim 全面改造

**背景**：分析了 GitHub 上成功运行的 `h3ct0r/ugv_gazebo_sim` Scout Mini 仿真项目，发现核心差异：

| 对比项 | 我们的旧方案 | ugv_gazebo_sim |
|--------|-------------|----------------|
| 驱动方式 | 后轮驱动 + 前轮 mimic | **四轮全部直驱** |
| 轮子轴 | Y 轴 + rpy 翻转 | **Z 轴** (`0 0 1` / `0 0 -1`) |
| 摩擦参数 | 大量自定义 mu/mu2/slip/kd/kp | **完全不设**，引擎默认 |
| 物理引擎 | ODE/DART 切换 + cfm/erp/iters | ODE 默认参数 |

**改造内容**：
1. 去掉所有 mimic 关节，四轮独立直驱
2. 轴方向统一为 Z 轴：左轮 `axis="0 0 1"` joint `rpy="-1.57 0 0"`，右轮 `axis="0 0 -1"` joint `rpy="1.57 0 0"`
3. DiffDrive 同时驱动四个关节（`left_joint` 指定 `front_left_wheel` + `rear_left_wheel`，`right_joint` 同理）
4. 删除所有轮子 `<surface>`/`<friction>`/`<contact>` 自定义参数
5. 删除地面 friction/contact 自定义参数
6. 简化 world 物理配置（去掉 cfm/erp/solver iters）
7. 新增 `max_wheel_torque=20` 和 `max_linear_acceleration=1.0`
8. wheel 宏新增 `collision_rpy` 参数支持

**状态**：🔄 改造完成，成功

---

## 修改文件清单

| 文件 | 修改内容 |
|------|----------|
| `scout_description/urdf/scout_wheel_type1.xacro` | collision_rpy 默认值支持；宏参数新增 collision_rpy |
| `scout_description/urdf/scout_wheel_type2.xacro` | 同上 |
| `scout_mini_dual_lidar_gazebo/urdf/scout_mini_gazebo.xacro` | 四轮直驱（去 mimic）、Z 轴、删除摩擦参数、DiffDrive 四关节、新增 torque/accel 限制 |
| `scout_mini_dual_lidar_gazebo/worlds/simple_test_world.world` | 简化物理配置、删除地面摩擦参数 |
| `scout_mini_dual_lidar_gazebo/launch/scout_mini_gazebo.launch.py` | 统一 bridge 消息类型为 gz.msgs、新增 /clock 桥接、所有节点添加 use_sim_time |
| `scout_mini_dual_lidar_gazebo/params/slam_toolbox_params.yaml` | scan_topic 改为 /front/scan、QoS 覆盖、transform_timeout/tolerance |
| `scout_mini_dual_lidar_gazebo/src/tf_static_relay.py` | **新建** — /tf_static 到 /tf 的中继节点 |
| `scout_mini_dual_lidar_gazebo/CMakeLists.txt` | 注册 tf_static_relay.py |

---

---

## 问题 12：World 插件冲突——Gazebo 一片空白 / 机器人不动 / 雷达无数据

**现象（三选一）**：
- 不写任何插件 → 机器人可运动，但雷达无数据
- 只写 Sensors → Gazebo 一片空白
- 写 Sensors + SceneBroadcaster → 雷达有数据，GUI 正常，但机器人不动

### 尝试过程

1. 保留 Sensors + SceneBroadcaster → 机器人不动（缺少 Physics 和 Contact）
2. 保留 Sensors + SceneBroadcaster + Contact → 仍不动（仍缺 Physics）
3. 添加 Physics → 机器人可运动但雷达又不行？等等

### 根因

Ignition Fortress 的 `server.config` 默认加载 Physics + UserCommands + SceneBroadcaster。**一旦 world 文件中有任何显式 `<plugin>` 声明，Gazebo 就停止加载默认插件，只加载显式声明的。** 所以必须显式写出全部 5 个必需插件。

此外，原始文件使用了 `gz-sim-*` 命名，但 Ignition Fortress 实际识别的是 `ignition-gazebo-*`。

### 最终方案

在 `simple_test_world.world` 中显式声明全部 5 个系统插件：

```xml
<plugin filename="ignition-gazebo-sensors-system" name="gz::sim::systems::Sensors"> ... </plugin>
<plugin filename="ignition-gazebo-scene-broadcaster-system" name="gz::sim::systems::SceneBroadcaster"> </plugin>
<plugin filename="ignition-gazebo-user-commands-system" name="gz::sim::systems::UserCommands"> </plugin>
<plugin filename="ignition-gazebo-contact-system" name="gz::sim::systems::Contact"> </plugin>
<plugin filename="ignition-gazebo-physics-system" name="gz::sim::systems::Physics"> </plugin>
```

**状态**：✅ 已解决

---

## 问题 13：Gazebo 模型名前缀导致 TF 树断裂

**现象**：Gazebo 的 diff drive 插件发布 TF 时自动加上了模型名前缀 `scout_mini/`：
- `scout_mini/odom → scout_mini/base_footprint`（Gazebo 发布）
- `base_link → base_footprint → front_lidar_link`（robot_state_publisher 发布）

两棵 TF 子树完全独立，没有连接点。RViz 中 Fixed Frame 下拉只有 `scout_mini/odom` 没有 `odom`，机器人模型不显示，SLAM 无法工作。

### 尝试过程

1. 尝试修改 diff drive 的 `odom_frame` 为不带前缀的 `odom` → Gazebo 仍然自动加前缀，无效
2. 尝试用 `remap` 话题 → 无效，这是 TF frame_id 问题不是话题名问题

### 最终方案

添加两个身份静态 TF 变换桥接两棵树：

```
odom  →  scout_mini/odom           (identity, static)
scout_mini/base_footprint  →  base_footprint   (identity, static)
```

完整 TF 链：
```
map → odom → scout_mini/odom → scout_mini/base_footprint → base_footprint → base_link → sensors
```

**关键发现**：diff drive 插件在 xacro 中配置的 `<robot_base_frame>base_footprint</robot_base_frame>`，所以 Gazebo 实际发布的是 `scout_mini/base_footprint`，不是 `scout_mini/base_link`。之前错误地桥接了 `scout_mini/base_link → base_link`，连接了不存在的帧。

**状态**：✅ 已解决

---

## 问题 14：ROS-Gazebo Bridge 消息类型命名空间不一致

**现象**：`/odom`、`/tf` 桥接可能无法正常工作。

### 根因

`scout_mini_gazebo.launch.py` 中混用了两种命名空间：
- `/cmd_vel@geometry_msgs/msg/Twist]gz.msgs.Twist`（`gz.msgs` 前缀）
- `/odom@nav_msgs/msg/Odometry[ignition.msgs.Odometry`（`ignition.msgs` 前缀）

Gazebo 实际发布的消息类型：`ign topic -i -t /odom` 显示 `ignition.msgs.Odometry`，`ign topic -i -t /tf` 显示 `ignition.msgs.Pose_V`。

### 最终方案

统一规则：ROS→Gazebo 用 `]gz.msgs.*`，Gazebo→ROS 用 `[ignition.msgs.*`：

```python
'/cmd_vel@geometry_msgs/msg/Twist]gz.msgs.Twist',
'/odom@nav_msgs/msg/Odometry[ignition.msgs.Odometry',
'/tf@tf2_msgs/msg/TFMessage[ignition.msgs.Pose_V',
'/joint_states@sensor_msgs/msg/JointState[ignition.msgs.Model',
'/front/scan@sensor_msgs/msg/LaserScan[gz.msgs.LaserScan',
'/rear/scan@sensor_msgs/msg/LaserScan[gz.msgs.LaserScan',
```

**状态**：✅ 已解决

---

## 问题 15：SLAM 地图保存失败

**现象**：
- 第一次：`Failed to spin map subscription` — 话题 `/map` 不存在
- 第二次：`Failed to write map ... magick/blob.c:3089 (OpenBlob)` — `/ws/maps/` 目录不存在

### 排查过程

1. `ros2 topic list | grep map` → 显示 `/scout_mini_map` 而非 `/map`
2. 根因：`slam_toolbox_params.yaml` 中 `map_name: scout_mini_map` 导致话题名为 `/scout_mini_map`

### 最终方案

1. 修改 `map_name` 为 `map`（符合 Nav2 默认约定）
2. 创建目录 `mkdir -p /ws/maps`

**状态**：✅ 已解决

---

## 问题 16：地图质量差——大片灰色、偏移、障碍物缺失

**现象**：RViz 中显示的地图有大片灰色（未知区域）、机器人初始位置偏在地图外。

### 分析

SLAM 建图时移动太快、区域覆盖不全、没有回环闭合，导致地图质量差。

### 尝试过的方法

1. 重新建图但只走外围一圈 → 中心大面积灰色
2. 尝试直接用它跑 Nav2 → AMCL 定位失败，粒子发散

### 最终方案

重新以低速（0.2-0.3 m/s）全覆盖建图，多走回环，每个角落从不同方向经过至少两次，确认 RViz 中无大片灰色后再保存。

**状态**：未解决

---

## 问题 17：Nav2 的 robot_state_publisher 重复导致 TF 冲突

**现象**：`There are 2 nodes in the graph with the exact name '/robot_state_publisher'`，导致 TF 树中缺少 `base_link → base_footprint`。

### 根因

`nav2_params.yaml` 中有独立的 `robot_state_publisher` 配置段（只有 `use_sim_time`，没有 URDF 的 `robot_description`），启动 Nav2 后可能与 Gazebo 仿真中的 robot_state_publisher 冲突。

### 最终方案

从 `nav2_params.yaml` 中删除 `robot_state_publisher` 配置段（Gazebo 启动文件中的 `robot_state_publisher` 已经提供了完整的 URDF 静态 TF）。

**状态**：✅ 已解决

---

## 问题 18：LiDAR 静态 TF 偏移量导致 LaserScan 显示位置错误

**现象**：RViz 中 LaserScan 不围绕机器人显示，偏移到了错误位置。

### 根因

`scout_mini_gazebo.launch.py` 中 LiDAR 静态 TF 写了 `0.245, 0, 0.14` 的偏移。但 Gazebo 传感器已经在 `<gazebo reference="front_lidar_link">` 下（`<pose>0 0 0 0 0 0</pose>`），传感器帧在 Gazebo 中已经在 URDF link 的位置。多余的静态 TF 偏移导致激光数据偏移了 2 倍距离。

### 最终方案

将前后 LiDAR 静态 TF 改为身份变换（全零）：
```
front_lidar_link  →  scout_mini/base_link/front_lidar_sensor  (0,0,0,0,0,0)
rear_lidar_link   →  scout_mini/base_link/rear_lidar_sensor   (0,0,0,0,0,0)
```

**状态**：✅ 已解决

---

## 问题 19：RViz 中轮子不显示且陷入地图

**现象**：RViz Fixed Frame 选 `odom` 时，`no transform from odom to base_link`，机器人模型不完整（轮子不显示），整体陷在地图中。

### 根因

问题 13 的延续——`model_prefix_tf_base` 连接了错误的帧名（`scout_mini/base_link` 而非 `scout_mini/base_footprint`），TF 链断裂导致 base_link 位姿计算错误。

此外 `robot_state_publisher` 重复（问题 17）使其可能丢失 URDF 中的静态 TF 信息。

### 最终方案

1. 将 `model_prefix_tf_base` 改为 `model_prefix_tf_base_footprint`，连接 `scout_mini/base_footprint → base_footprint`
2. 删除 `nav2_params.yaml` 中的 `robot_state_publisher` 段

**状态**：✅ 已解决

---

## 经验总结

1. **滑移转向（skid-steer）在 ODE 物理引擎中是公认的难题**，圆柱体碰撞体与平面的线接触无法产生足够的侧向力差来驱动转向。
2. **摩擦参数调优是歧路**：反复调整 mu/mu2/slip/contact 参数只能微调行为，无法解决根本性的物理仿真限制。
3. **mimic 关节在 Ignition Fortress 中不可靠**：应避免使用，改用多关节直驱。
4. **参考成功案例比盲目试错高效得多**：ugv_gazebo_sim 的四轮直驱+Z轴+无自定义摩擦参数方案是一个经过验证的可行路径。
5. **TF 树的双源冲突（DiffDrive 动态 + URDF 静态同时定义 base_footprint）是 90 度卡住的可能根因之一**。
6. **Z 轴比 Y 轴更适合 DiffDrive 插件**：Z 轴方案不需要 rpy 翻转，左右方向映射更直观可靠。
7. **World 显式声明插件会抑制默认加载**：必须显式写出所有必需插件（Physics、Contact、Sensors、SceneBroadcaster、UserCommands），并用 `ignition-gazebo-*` 命名。
8. **Gazebo 模型名前缀问题（`scout_mini/`）**：diff drive 等插件自动添加模型名前缀到 frame_id，需用静态 TF 桥接到标准 ROS 坐标系。
9. **静态 TF 应使用身份变换**：当 Gazebo 传感器已放在 URDF link 位置时（`<pose>0 0 0 0 0 0</pose>`），桥接静态 TF 必须是全零。
10. **diff drive 的 `robot_base_frame` 决定 TF 树的正确连接点**：如果 xacro 中设为 `base_footprint`，桥接必须对 `base_footprint` 而非 `base_link`。
11. **同名节点冲突（`robot_state_publisher`）**：Nav2 yaml 参数不能包含与 Gazebo 仿真重复的节点配置。
12. **SLAM 的 `map_name` 应保持默认 `map`**：避免与 Nav2 的默认话题名不一致。
